---
- name: Restart k3s
  ansible.builtin.systemd:
    name: k3s
    state: restarted
    daemon_reload: true
  # On first install the unit doesn't exist until the installer runs.
  failed_when: false
