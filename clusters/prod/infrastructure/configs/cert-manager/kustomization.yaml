---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - cluster-issuer.yaml
  # Uncomment once the SOPS-encrypted secret exists (see
  # cloudflare-api-token-secret.sops.yaml.example):
  # - cloudflare-api-token-secret.sops.yaml
