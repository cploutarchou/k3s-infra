---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - cluster.yaml
  - scheduled-backup.yaml
  # Uncomment once the SOPS-encrypted secret exists (see
  # r2-backup-credentials.sops.yaml.example):
  # - r2-backup-credentials.sops.yaml
