---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Secret and namespace only for now; the HelmRelease for mcp/chart lands
# here once the image is built and digest-pinned.
resources:
  - namespace.yaml
  - mcp-server-secret.sops.yaml
