---
# nginx serving the built React bundle. The stock image needs no staging
# rebuild: frontend/src/api/origin.ts classifies staging.executionlab.io as
# the staging environment, rejects the build-time production API base for
# that environment, and falls through to https://api.staging.executionlab.io.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: executionlab-staging
  labels:
    app.kubernetes.io/name: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: frontend
  template:
    metadata:
      labels:
        app.kubernetes.io/name: frontend
    spec:
      containers:
        - name: frontend
          image: ghcr.io/cploutarchou/dydx-trading-bot/frontend:staging-c1082651@sha256:69923101a29173205e71cf79e858f78808150a278b1f45461541a417b8bfa2f0
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          resources:
            requests:
              cpu: 25m
              memory: 32Mi
            limits:
              cpu: 200m
              memory: 96Mi
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: executionlab-staging
spec:
  selector:
    app.kubernetes.io/name: frontend
  ports:
    - name: http
      port: 80
      targetPort: http
